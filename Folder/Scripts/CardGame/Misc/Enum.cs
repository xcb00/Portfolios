public enum SceneName
{
    Manager, Regist, Load, Lobby, Single, Multi, LeaveRoom, None
}

public enum TableType
{
    Max, Min, Gap
}

public enum Difficulty
{
    Easy, Normal, Hard
}

public enum GameResult
{
    Lose = -1, Draw = 0, Win = 1
}